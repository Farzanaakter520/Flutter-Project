name: hospital_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.4.0
  image: ^4.5.4
  image_picker: ^1.1.2
  path: ^1.9.1
  path_provider: ^2.1.5
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  intl: ^0.18.1

flutter:
  uses-material-design: true

  assets:
    - images/
